# Youtube downloader. 

### Dependencies 
* go - v1.12
* brew install ffmpeg (OSX)
* apt install ffmpeg (Linux)

### Build command
```
$ export SSH_PRIVATE_KEY="$(cat ../id_rsa)"
$ make build
$ make push
```