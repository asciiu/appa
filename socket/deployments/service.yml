apiVersion: v1
kind: Service
metadata:
  name: socket 
  labels:
    app: socket 
spec:
  type: LoadBalancer
  ports:
  - name: http
    protocol: TCP
    port: 5000 
    targetPort: 5000
    nodePort: 32000
  selector:
    app: socket 